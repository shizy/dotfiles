general {
        colors = true
        interval = 5
        color_good = "#585858"
        color_bad = "#B1D631"
        color_degraded = "#B1D631"
}

order += "wireless _first_"
order += "volume master"
order += "battery all"
order += "tztime local"

wireless _first_ {
        format_up = " %essid"
        format_down = ""
}

battery all {
        format_down = ""
        status_chr = " "
        status_bat = " "
        format = "%status %percentage"
        low_threshold = 15
        threshold_type = percentage
}

tztime local {
        format = "  %h %d, %I:%M%P"
}

volume master {
        format = "  %volume"
        format_muted = ""
        device = "default"
        mixer = "Master"
        mixer_idx = 0
}
